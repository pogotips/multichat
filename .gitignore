node_modules/
.wrangler/

# Real config — copy the .example files and fill in your own values.
# The .example variants are committed; these real ones stay local.
.dev.vars
wrangler.jsonc
docker-compose.yml
tools/yt-poller/docker-compose.yml
